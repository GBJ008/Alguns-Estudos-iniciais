Como mecher no Git alguns códigos para ser anotados 

untracked= ainda não foi visto pelo git( ou não foi "salvo")
assim ele pode ser add the file ou remove the file

unmodified= sem nem uma modificação no código 
por tanto ele pode ser edit the file e pode passar para o modified qque no caso é que o arquivo foi modificado 

modified=  foi editado mas não foi salvo ainda 
assim ele pode ir para o staged que é a fase final que ele esta pronto para ser comitado 


staged= esta pronto para ser comitado (ser salvo )


CÒDIGOS 

ls= lista dos arquivos
git status= Ver como os arquivos estão 
git add= Adicionar o arquivo 
code = vscode
letra i é o editor 
:wq = é para sair e salvar o arquivo 
git commit -m "" ( aqui você comenta qual foi a edição que você fez no arquivo, para manter sempre atualizado
)
git log= ver o que mudou nós arquivos(quando mudou e quem mudou)
git log --decorate = vai mostra o que mudou com mais detalhhes
git log --author=' ' = mostra a pessoa que fez a alteração tipo um filtro para achar o author (o nome que estiver entre aspas )
git shortlog = mostra quais os auores e quantos comites fizeram e quais eles foram
git shortlog -sn = só a quantidade de commit e quem commito
git log --graph = ele vai mostra em forma grafica o que esta acontecendo com os brands e versões 
git show (o código do commit que aparece no git log)= vai mostra o que foi adicionado em um commit especifico de uma forma mais profunda 
git push -u origin main = para enviar remotamente o que foi adicionado no commit( o origin pode ser mudado ele serve para direcionar para qual repositoriao você querq)
git commit -am "" = o -a serve para adicionar direto.
